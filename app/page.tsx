"use client";

import Image from "next/image";
import { FormEvent, useEffect, useState } from "react";

const steps = [
  ["01", "Audit the gym", "Map how enquiries, trials, memberships and follow-up currently move through the business."],
  ["02", "Find the leaks", "Identify where prospects wait, staff repeat admin or opportunities quietly disappear."],
  ["03", "Prioritise", "Choose the few automations most likely to improve the member journey or remove staff workload."],
  ["04", "Build", "Create the workflow around the gym instead of forcing the gym around the technology."],
  ["05", "Improve", "Review the system and refine the handoffs as the gym grows."],
];

const solutions = [
  ["01", "Turn enquiries into booked trials", "Respond faster, capture useful context and move interested prospects towards the next step.", "Lead", "Trial", "Staff"],
  ["02", "Follow up without chasing", "Create consistent follow-up around trial bookings, missed enquiries and prospects who are not ready yet.", "Enquiry", "Follow-up", "Handoff"],
  ["03", "Remove front-desk repetition", "Reduce routine data entry, internal notifications and repetitive member admin.", "Task", "System", "Done"],
  ["04", "Keep the member journey connected", "Make it clearer what should happen between first enquiry, first visit and becoming a member.", "Interest", "Visit", "Member"],
];

const diagnosticCopy: Record<string,string> = {
  "New enquiries":"Capture the lead, collect useful context and get the right person involved before interest goes cold.",
  "Trial follow-up":"Trigger consistent follow-up after a trial request or visit while keeping staff in control of the conversation.",
  "Missed leads":"Surface enquiries that have not received the next action instead of relying on someone remembering them.",
  "Member admin":"Reduce repetitive updates, routing and notifications around routine member operations.",
  "Lead qualification":"Collect the information your team needs before spending time on the conversation.",
  "Reporting":"Bring recurring lead and operational information into a clearer workflow for the gym owner."
};

function Arrow(){return <span className="arrow" aria-hidden="true">↗</span>}
function Brand(){return <span className="brand-lockup"><Image src="/chatgpt_image_jan_27,_2026,_04_29_01_am.png" alt="Booth Marketing" width={360} height={110} priority className="brand-logo"/></span>}

export default function Home(){
  const [menu,setMenu]=useState(false); const [submitted,setSubmitted]=useState(false); const [sending,setSending]=useState(false); const [error,setError]=useState(""); const [activeStep,setActiveStep]=useState(0); const [diagnostic,setDiagnostic]=useState("New enquiries");
  useEffect(()=>{const items=Array.from(document.querySelectorAll<HTMLElement>("[data-process-step]"));const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)setActiveStep(Number((visible.target as HTMLElement).dataset.processStep||0));},{rootMargin:"-30% 0px -45% 0px",threshold:[.2,.5]});items.forEach(i=>observer.observe(i));return()=>observer.disconnect();},[]);
  async function handleSubmit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSending(true);setError("");const form=new FormData(e.currentTarget);const payload={name:String(form.get("name")||""),business:String(form.get("business")||""),email:String(form.get("email")||""),friction:String(form.get("friction")||"")};try{const r=await fetch("/api/audit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});if(!r.ok)throw new Error();setSubmitted(true)}catch{setError("Something went wrong. Please try again.")}finally{setSending(false)}}
  const close=()=>setMenu(false);
  return <main id="top" className="gym-site"><div className="page-grid" aria-hidden="true"/><div className="energy-orb energy-one" aria-hidden="true"/><div className="energy-orb energy-two" aria-hidden="true"/>
    <header className="site-header-wrap"><div className="site-header shell"><a className="brand" href="#top" aria-label="Booth Marketing home"><Brand/></a><nav className="desktop-nav" aria-label="Primary"><a href="#solutions">Gym Systems</a><a href="#approach">Member Journey</a><a href="#work">Approach</a><a href="#audit">Free Audit</a></nav><a className="header-cta" href="#audit">Audit My Gym <Arrow/></a><button className="menu-button" type="button" aria-label="Open menu" aria-expanded={menu} onClick={()=>setMenu(!menu)}><span/><span/></button></div>{menu&&<nav className="mobile-nav shell" aria-label="Mobile"><a onClick={close} href="#solutions">Gym Systems</a><a onClick={close} href="#approach">Member Journey</a><a onClick={close} href="#work">Approach</a><a onClick={close} href="#audit">Audit My Gym <Arrow/></a></nav>}</header>

    <section className="hero shell"><div className="hero-copy-wrap"><p className="eyebrow"><span className="status-dot"/> AUTOMATION SYSTEMS FOR INDEPENDENT GYMS</p><h1>More members. <span>Less admin.</span></h1><p className="hero-copy">Booth Marketing helps gym owners turn enquiries into trials, follow up consistently and remove repetitive operational work without replacing the human side of the gym.</p><p className="hero-subcopy">Built for serious gym operators who want a stronger system behind the member journey.</p><a className="primary-cta" href="#audit">Audit My Gym <Arrow/></a><div className="hero-trust"><span>Gym-focused review</span><span>Practical recommendations</span><span>No sales pressure</span></div></div>
      <div className="ops-canvas gym-canvas" aria-label="Illustrative gym member acquisition workflow"><div className="power-label">MEMBER ACQUISITION SYSTEM <b>LEVEL 04</b></div><div className="ops-topline"><span>GYM GROWTH FLOW</span><span className="live-pill"><i/> SYSTEM ACTIVE</span></div><div className="ops-stage"><div className="connector connector-a"><b/></div><div className="connector connector-b"><b/></div><div className="connector connector-c"><b/></div><div className="ops-node node-1"><small>01</small><strong>Enquiry lands</strong><span>Website · Social · Referral</span></div><div className="ops-node node-2"><small>02</small><strong>Trial booked</strong><span>Context · Timing · Intent</span></div><div className="ops-node node-3"><small>03</small><strong>Follow-up runs</strong><span>Reminder · Status · Owner</span></div><div className="ops-node node-4"><small>04</small><strong>Staff handoff</strong><span>Right lead · Right moment</span></div></div><div className="ops-footer"><span><i/> Faster response</span><span><i/> Fewer missed leads</span><span><i/> Clear handoffs</span></div></div>
    </section>

    <section className="proof-strip section-line"><div className="shell"><span>Built for gym operations</span><span>Member experience stays human</span><span>Systems before software</span></div></section>
    <section className="editorial-break section-line"><div className="shell"><p>YOUR GYM SHOULD NOT RELY ON MEMORY.</p><h2>Every enquiry that sits unanswered and every trial that gets no follow-up is a member opportunity your system should have protected.</h2></div></section>

    <section className="section shell section-line" id="solutions"><div className="section-heading"><p className="eyebrow">GYM AUTOMATION SYSTEMS</p><h2>Build the operation behind a stronger member journey.</h2><p>The goal is not to automate your coaches. It is to remove the repetitive work around them so the team can focus on members.</p></div><div className="solution-grid">{solutions.map(([n,title,copy,a,b,c])=><article className="solution-card" key={n}><span>{n}</span><h3>{title}</h3><p>{copy}</p><div className="solution-flow"><i>{a}</i><b>→</b><i>{b}</i><b>→</b><i>{c}</i></div></article>)}</div></section>

    <section className="workflow-showcase section-line" id="approach"><div className="shell"><div className="section-heading"><p className="eyebrow">THE MEMBER JOURNEY</p><h2>From “I’m interested” to a clear next action.</h2></div><div className="before-after"><article><span>WITHOUT A SYSTEM</span><div className="flow-stack bad"><i>Instagram enquiry</i><i>Staff notices later</i><i>Details copied manually</i><i>Trial happens</i><i>Follow-up forgotten</i></div></article><div className="transform-mark">→</div><article><span>WITH A SYSTEM</span><div className="flow-stack good"><i>Enquiry captured</i><i>Context collected</i><i>Trial booked</i><i>Follow-up triggered</i><i>Staff closes personally</i></div></article></div></div></section>

    <section className="project-section section-line" id="work"><div className="shell"><div className="section-heading"><p className="eyebrow">WHAT WE DESIGN AROUND</p><h2>Your gym already has a sales process. We make it harder for leads to fall through it.</h2></div><div className="project-grid"><article className="project-feature"><span>MEMBER ACQUISITION</span><h3>From first interest to a useful staff conversation.</h3><div className="project-diagram"><i>Enquiry</i><b>→</b><i>Context</i><b>→</b><i>Trial</i><b>→</b><i>Handoff</i></div><p>A cleaner route from prospect to staff, with repetitive steps handled consistently.</p></article><article><span>TRIAL FOLLOW-UP</span><h3>Visit → follow-up → decision</h3><p>Give every trial a defined next action instead of leaving it to memory.</p></article><article><span>GYM OPERATIONS</span><h3>Routine admin → system</h3><p>Reduce repetitive work around leads, member updates and internal handoffs.</p></article></div></div></section>

    <section className="thinking-section section-line"><div className="shell thinking-layout"><div className="thinking-sticky"><p className="eyebrow">OUR METHOD</p><h2>Train the system. Keep the people.</h2><p>Your coaches, staff and community are the gym. Automation should support them, not get between them and the member.</p><div className="process-progress"><span style={{width:`${((activeStep+1)/steps.length)*100}%`}}/></div><div className="process-count">0{activeStep+1} <span>/ 0{steps.length}</span></div></div><div className="process-list">{steps.map(([n,title,copy],i)=><article className={`process-step ${activeStep===i?"active":""}`} data-process-step={i} key={n}><span className="process-number">{n}</span><div><h3>{title}</h3><p>{copy}</p></div><div className="process-orbit"><i/></div></article>)}</div></div></section>

    <section className="guardrails section-line"><div className="shell guardrails-grid"><div><p className="eyebrow">THE RULE</p><h2>Automate the admin. Not the culture.</h2></div><div><p>Keep coaching human.</p><p>Keep important sales conversations human.</p><p>Keep member relationships human.</p><p>Automate repeatable steps around those moments.</p></div></div></section>

    <section className="diagnostic section-line"><div className="shell diagnostic-layout"><div><p className="eyebrow">GYM SYSTEM CHECK</p><h2>Where is your gym losing time or leads?</h2><div className="diagnostic-tabs">{Object.keys(diagnosticCopy).map(item=><button type="button" className={diagnostic===item?"active":""} onClick={()=>setDiagnostic(item)} key={item}>{item}</button>)}</div></div><div className="diagnostic-result"><span>POSSIBLE OPPORTUNITY</span><p>{diagnosticCopy[diagnostic]}</p><a href="#audit">Review this in my gym audit <Arrow/></a></div></div></section>

    <section id="audit" className="audit-section section-line"><div className="shell audit-shell"><div className="audit-copy"><p className="eyebrow">FREE GYM AUTOMATION AUDIT</p><h2>Find the leaks in your member journey.</h2><p>We review how enquiries, trials, follow-up and repetitive admin currently move through your gym and identify where a better system could help.</p><div className="audit-list"><span>Lead journey review</span><span>Follow-up gaps</span><span>Admin bottlenecks</span><span>Priority recommendations</span></div><p className="reassurance">Useful insight even if we never work together.</p></div><form className="audit-form" onSubmit={handleSubmit}>{submitted?<div className="form-success" role="status"><span>GYM AUDIT REQUEST RECEIVED</span><h3>Thanks. We’ll take it from here.</h3><p>We’ll review the context you shared and get in touch about the next step.</p></div>:<><div className="form-head"><span>AUDIT MY GYM</span><small>~ 2 minutes</small></div><div className="form-row"><label>Your name<input required name="name" autoComplete="name" placeholder="Your name"/></label><label>Gym name<input required name="business" autoComplete="organization" placeholder="Gym name"/></label></div><label>Work email<input required type="email" name="email" autoComplete="email" placeholder="you@gym.com"/></label><label>Where are you losing time or leads?<textarea required name="friction" rows={5} placeholder="For example: trial follow-up, missed enquiries, repetitive admin..."/></label><button className="primary-cta form-button" type="submit" disabled={sending}>{sending?"Sending…":"Audit My Gym"} {!sending&&<Arrow/>}</button>{error&&<p className="form-error" role="alert">{error}</p>}<p className="form-note">No sales pressure. No obligation.</p></>}</form></div></section>

    <section className="section shell section-line next-steps"><div className="section-heading"><p className="eyebrow">WHAT HAPPENS NEXT</p><h2>A practical review of the systems behind your gym.</h2></div><div className="timeline">{[["01","Intro call","A focused conversation about the gym."],["02","Journey review","We map enquiries, trials and handoffs."],["03","Find the leaks","We identify repetitive work and missed opportunities."],["04","Recommendations","You get a practical priority view."],["05","You decide","Use the recommendations however you choose."]].map(([n,t,c])=><article className="timeline-step" key={n}><span>{n}</span><i/><h3>{t}</h3><p>{c}</p></article>)}</div></section>
    <section className="final-cta section-line"><div className="final-glow"/><div className="shell final-inner"><p className="eyebrow">FREE GYM AUTOMATION AUDIT</p><h2>Stop letting good leads disappear between sets.</h2><p>Find the operational leaks. Get practical recommendations. Keep the human side of your gym human.</p><a className="primary-cta" href="#audit">Audit My Gym <Arrow/></a></div></section>
    <footer className="footer shell"><a className="brand" href="#top" aria-label="Booth Marketing home"><Brand/></a><p>Automation systems for independent gyms.</p></footer>
  </main>
}
